# CH2-06A5

Replace the included files using their existing paths.

Install/update PDF dependencies:

```powershell
python -m pip install -r requirements-ch2-06a5.txt
```

Restart backend and frontend after replacement.
